import 'package:flutter/material.dart';

class AppAnimateScreen extends StatelessWidget {
  const AppAnimateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}